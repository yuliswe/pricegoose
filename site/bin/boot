#!/usr/bin/env bash
set -xe
service postgresql start
wait-for-it localhost:5432
manage runserver --force-color 0.0.0.0:80
# runserver is expected to run forever
# sleep for supervisord restart
sleep 3
exit 1
