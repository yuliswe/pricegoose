version: "3.7"

services:
  # db:
  #   build: ./db
  #   hostname: db
  #   ports:
  #     - 8432:5432

  site:
    build:
      context: ./site
      shm_size: "128MB"
    restart: always
    hostname: site
    volumes:
      - ./site:/root/site:ro
    ports:
      - 8080:80
